<h1><%= @article.title %></h1>

<h3><%= @article.content %></h3>

<%= link_to "Delete #{@article.title}",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>